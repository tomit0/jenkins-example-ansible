pipeline {
  agent {label 'linux'}

  stages {
    stage('Hello') {
      steps {
        sh '''
          uname -a
          apt-get update
          apt-get install ansible
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}
